<%= link_to "Zoos", '/zoos' %>
<%= link_to "Volunteers", '/volunteers' %>

<h1>All Volunteers</h1>

<% @volunteers.each do |volunteer|%>
  <h3><%= volunteer.name %></h3>
  <h4>Certified? <%= volunteer.certified_trainer %></h4>
  <h4>Years Experience: <%= volunteer.years_of_experience %></h4>
  <h4><p>ID: <%= volunteer.id %></p></h4>
  <h4><p>Created on: <%= volunteer.created_at %></p></h4>
  <h4><p>Updated on: <%= volunteer.updated_at %></p></h4>
  <p><%= link_to "Update Volunteer", "/volunteers/#{volunteer.id}/edit" %></p><br>
  <%= button_to "Delete Volunteer", "/volunteers/#{volunteer.id}", method: :delete%>
<% end %>
